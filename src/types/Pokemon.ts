export interface Pokemon {
  id: number;
  name: string;
  spriteUrl: string;
}

// Nota: Manteremos o nome do arquivo para evitar quebrar referências existentes,
// mas a interface é agora usada para representar criaturas genéricas
